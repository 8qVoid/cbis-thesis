@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between mb-3"><div><div class="cbis-eyebrow">Reservation</div><h1 class="cbis-page-title">{{ $reservation->reference }}</h1></div><a href="{{ route('reservations.index') }}" class="btn btn-outline-secondary">Back</a></div>
<div class="row g-4"><div class="col-lg-7"><div class="card"><div class="card-body"><dl class="row mb-0"><dt class="col-sm-4">Patient</dt><dd class="col-sm-8">{{ $reservation->patient->name }}</dd><dt class="col-sm-4">Branch</dt><dd class="col-sm-8">{{ $reservation->facility->name }}</dd><dt class="col-sm-4">Request</dt><dd class="col-sm-8">{{ $reservation->units_requested }} unit(s) of {{ $reservation->blood_type }} {{ \App\Models\BloodInventory::COMPONENTS[$reservation->component] }}</dd><dt class="col-sm-4">Needed on</dt><dd class="col-sm-8">{{ $reservation->needed_on->toDateString() }}</dd><dt class="col-sm-4">Status</dt><dd class="col-sm-8">{{ str($reservation->status)->replace('_',' ')->title() }}</dd><dt class="col-sm-4">Notes</dt><dd class="col-sm-8">{{ $reservation->review_notes ?: '—' }}</dd></dl></div></div>@if(auth()->user()->hasRole('Patient') || auth()->user()->can('process reservations'))<div class="card mt-4"><div class="card-header fw-bold">Submitted Documents</div><div class="list-group list-group-flush">@foreach($reservation->documents as $document)<a class="list-group-item list-group-item-action" href="{{ route('reservations.documents.show',[$reservation,$document->id]) }}">{{ str($document->type)->replace('_',' ')->title() }} — {{ $document->original_name }}</a>@endforeach</div></div>@else<div class="alert alert-secondary mt-4 mb-0">Patient documents are restricted to the assigned Blood Bank Staff.</div>@endif</div>
@can('process reservations')@php($allowedTransitions = ['submitted'=>['under_review','rejected'],'under_review'=>['approved','rejected'],'approved'=>['fulfilled','rejected']][$reservation->status] ?? [])<div class="col-lg-5">@if($allowedTransitions)<form method="POST" action="{{ route('reservations.review',$reservation) }}" class="card"><div class="card-header fw-bold">Blood Bank Staff Review</div><div class="card-body">@csrf @method('PATCH')<label class="form-label">Status</label><select name="status" class="form-select mb-3">@foreach($allowedTransitions as $status)<option value="{{ $status }}">{{ str($status)->replace('_',' ')->title() }}</option>@endforeach</select><label class="form-label">Review Notes</label><textarea name="review_notes" class="form-control mb-3" rows="4">{{ old('review_notes',$reservation->review_notes) }}</textarea><button class="btn btn-danger">Update Status</button></div></form>@else<div class="alert alert-secondary">This reservation is complete and cannot be changed.</div>@endif</div>@endcan</div>
@endsection
